now i have been able to complete the 8 bit adder and sub and the multiplier but i will write how i built them 
first i learned about every gate the neccessary ones : the no , and , or , xor ,nand , nor and xor
then i started playing with them i tried to build the 4 bit adder my self and it was big and not easy to debug i will put all the photos 
then i coulden't build a subtractor so i used the truth table in digital.exe
the subtractor was bigger then the whole calculator
it looked like this will be impossible to build in reel life so i searched for how to build one and it was 100 times smaller
then i connected everything to a 4 bit input
i tried building a multiplier then i gave up and looked in the internet
when i finished i built a 4 bit encoder from 1 to 15 and done and i had a 3 buttons for operations
i turned the 4 bit adder to 8 bit then learned how to add negative numbers i didnt use the normal way i actually used to take the output invert them and add one
